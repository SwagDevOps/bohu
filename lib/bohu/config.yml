---
shell:
  verbose: true
utils:
  verbose: true
commands:
  usermod:
    actions:
      change_shell:
        login_shell: '%<login_shell>s'
        login: '%<login>s'
  adduser:
    actions:
      create_user:
        assign_password: false
        login_shell:
          - '%<login_shell>s'
          - '/bin/bash'
        login: '%<login>s'
